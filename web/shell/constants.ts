// The starting Planet name (server sets this at registration). Duplicated here because the web
// layer can't import server modules; kept trivial so the two can't drift meaningfully.
export const DEFAULT_PLANET_NAME = 'Homeworld';
